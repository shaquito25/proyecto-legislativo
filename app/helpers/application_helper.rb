module ApplicationHelper

    # 	def check_administrator_permission
	# 		current_user = User.find_by_id(session[:user_id])
	# 		unless current_user.perfil_id == 1
	# 			flash[:alert] = "No posee los permisos para acceder a esa pantalla"
	# 			redirect_to root_path
	# 		end
	# end
end
